import 'package:flutter/material.dart';

class WallTile extends StatelessWidget {
  const WallTile({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue[800],
    );
  }
}
